
[project]
name = "rtma"
version = "0.1.0"
description = "Real-Time Modal Analysis and Vibration Diagnosis Tool"
authors = [{name = "Your Name", email = "your.email@example.com"}]
dependencies = ["PyQt6", "numpy", "scipy", "matplotlib", "pyqtgraph", "scikit-learn"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
